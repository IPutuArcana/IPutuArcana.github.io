# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# Claude Code's local tool-permission cache. Machine-specific — it records
# absolute paths from whichever checkout approved them, so it is noise in a
# shared repository and doubly so in a public one.
.claude/

# Character motion clips. Kept out of the repository on purpose: the freely
# available .vrma packs forbid redistributing the data in a form that can be
# extracted or rigged, and both a public repo and a static host do exactly
# that. See public/models/motions/README.md before removing this.
public/models/motions/*.vrma
